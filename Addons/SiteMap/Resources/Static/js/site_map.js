define(["jquery", "easy-admin"], function ($, ea) {

    var init = {
        table_elem: '#currentTable',
        table_render_id: 'currentTableRenderId',
    };

    return {

        index: function () {
            ea.listen();
        },config: function () {
            ea.listen();
        },

    };
});
